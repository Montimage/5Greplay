<beginning>
<embedded_functions><![CDATA[

static void em_modify_cam(
      const rule_info_t *rule, int verdict, uint64_t timestamp,
      uint64_t counter, const mmt_array_t * const trace ){
    // Modify station Id
    set_numeric_value(PROTO_CAM,3, 75013);
    // Modify reference position
    set_numeric_value(PROTO_CAM,6, 0);
    set_numeric_value(PROTO_CAM,7, 0);
    forward_packet();
}
]]></embedded_functions>
<property property_id="12" type_property="FORWARD" description="Modify multiple attributes in a CAM message" if_satisfied="em_modify_cam">
    <event description="Identify a CAM packet" boolean_expression="((cam.protocolversion == 2 ) &amp;&amp;(cam.messageid == 2))"/>
</property>
</beginning>